<nav class="nav-links">
    <a class="nav-link @(Context.Request.Path == "/admin/projects" ? "active" : "")" asp-controller="Project" asp-action="Projects">
        <i class="bi bi-briefcase-fill"></i>
        <span>Projects</span>
    </a>
    @if (User.IsInRole("Admin"))
    {
           <a class="nav-link @(Context.Request.Path == "/admin/members" ? "active" : "")" asp-controller="Member" asp-action="Members">
        <i class="bi bi-people-fill"></i>
        <span>Team Members</span>
         </a>
    }
 
    @if (User.IsInRole("Admin"))
    {
        <a class="nav-link @(Context.Request.Path == "/admin/clients" ? "active" : "")" asp-controller="Client" asp-action="Clients">
            <i class="bi bi-star-fill"></i>
            <span>Clients</span>
        </a>
    }
 
</nav>